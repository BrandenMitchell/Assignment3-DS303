#ifndef ASSIGNMENT3_QUEUEIMPLEMENTATION_FUNCTIONS_H
#define ASSIGNMENT3_QUEUEIMPLEMENTATION_FUNCTIONS_H

#endif //ASSIGNMENT3_QUEUEIMPLEMENTATION_FUNCTIONS_H


#include <string>
#include <iomanip>
#include <vector>
#include <iostream>

using namespace std;


int linear_search(vector<int>& items, int& target, size_t pos_first, int& instance );

int linear_search_wrapper(vector<int>& items,int& target); //we will call wrapper which will call the func






